export const metadata = { title: 'Agent & Business Dashboard', description: 'Performance Analytics' }
export default function RootLayout({ children }) {
  return <html lang="en"><body style={{ margin: 0 }}>{children}</body></html>
}
